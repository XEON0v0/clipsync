[package]
name = "clipboard-server"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
axum.workspace = true
clipboard-core = { path = "../core", default-features = false, features = ["verify"] }
tokio.workspace = true
